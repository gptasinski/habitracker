<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="row">
      <% if current_user %>
        <div class="col-xs-4 col-md-4"><%= link_to 'Back', :back, class: "btn btn-default navbar-btn" %></div>
        <div class="col-xs-4 col-md-4 text-center logo">HABIT TRACKER</div>
        <div class="col-xs-4 col-md-4 text-right">
          Signed in as: <%= current_user.name %>
          <%= link_to 'Log Out', logout_path, class: "btn btn-default navbar-btn" %>
        </div>
      <% else %>
        <div class=" col-xs-4 col-md-4">
          <%= link_to 'Sign Up', new_user_path, class: "btn btn-default navbar-btn" %>
          <%= link_to 'Log In', login_path, class: "btn btn-default navbar-btn" %></div>
        <div class=" col-xs-4 col-md-4 text-center logo">HABIT TRACKER</div>
        <div class=" col-xs-4 col-md-4"></div>
      <% end %>
    </div>
  </div>
</nav>